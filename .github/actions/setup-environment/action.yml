name: Setup environment
description: Install uv, restore cache, sync deps from uv.lock
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v3
      with:
        enable-cache: true
        cache-dependency-glob: uv.lock
    - shell: bash
      run: uv sync --frozen
